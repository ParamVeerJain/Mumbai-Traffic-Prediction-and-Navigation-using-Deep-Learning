# --extra-index-url https://download.pytorch.org/whl/cu121

# torch
# torchvision
# torchaudio
osmnx
networkx 
pandas 
numpy
pyarrow
fastparquet
matplotlib
seaborn
geopy

networkx>=3.0
geopy>=2.4.0
dash>=2.17.0
dash-bootstrap-components>=1.5.0
plotly>=5.18.0
requests>=2.31.0
scipy>=1.11.0
lightgbm



fastapi>=0.109
uvicorn[standard]>=0.27
pydantic>=2.5
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
pyarrow>=14.0
torch>=2.1
osmnx>=1.9
networkx>=3.2
dash>=2.17
plotly>=5.24
numpy>=1.26
pandas>=2.1
requests>=2.31

# ── Frontend (required) ──────────────────────────────────────────────────────
PyQt6>=6.5

# ── Backend / generator (required for generate_pngs.py) ─────────────────────
# Install these in the same environment if you also run the generator.
# (They are almost certainly already installed since they were used during
# dataset creation & model training.)
numpy>=1.24
pandas>=2.0
pyarrow>=12.0
matplotlib>=3.7
osmnx>=1.9
shapely>=2.0
scikit-learn>=1.3           # for the StandardScaler used by generate_predictions.py
